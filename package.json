{
    "name": "aliexpress-order-exporter",
    "version": "1.1.0",
    "private": true,
    "description": "Build scripts for the AliExpress Order Exporter Chrome extension.",
    "scripts": {
        "build:zip": "node scripts/build.js zip",
        "build:crx": "node scripts/build.js crx",
        "build": "node scripts/build.js all"
    },
    "devDependencies": {
        "archiver": "^7.0.1",
        "crx": "^5.0.1"
    }
}
